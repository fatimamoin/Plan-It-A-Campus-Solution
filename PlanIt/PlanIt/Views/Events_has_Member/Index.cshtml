@model IEnumerable<PlanIt.Models.Events_has_Member>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_ClubsLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Club_member.Student_idStudent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Event.Club_idClub)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Club_member.Student_idStudent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Event.Club_idClub)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idEvents_has_Member }) |
            @Html.ActionLink("Details", "Details", new { id=item.idEvents_has_Member }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idEvents_has_Member })
        </td>
    </tr>
}

</table>
